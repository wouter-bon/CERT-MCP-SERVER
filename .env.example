# CERT-MCP-SERVER Environment Variables

# Configuration file path (optional - can run without config)
CERT_MCP_CONFIG=/path/to/config.json

# Cloudflare API token for DNS-01 challenges
# Required for Let's Encrypt certificate requests
# Token needs Zone:Read and DNS:Edit permissions
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# Let's Encrypt contact email
# Required for ACME account registration
ACME_EMAIL=admin@example.com

# ACME account key storage path (optional)
# If not set, a new key will be generated each time
ACME_ACCOUNT_KEY_PATH=~/.acme/account.key

# Use Let's Encrypt staging environment (optional)
# Set to "true" for testing to avoid rate limits
ACME_STAGING=false

# Log level (optional, default: INFO)
LOG_LEVEL=INFO

# Server settings (optional, can be set in config file)
CERT_MCP_HOST=0.0.0.0
CERT_MCP_PORT=8815
CERT_MCP_NAME=cert-mcp-server
